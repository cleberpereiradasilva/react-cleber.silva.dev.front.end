import {render} from '@testing-library/react'
import { {{pascalCase name}} } from "../{{pascalCase name}}";

describe('First test', () => {
    it('should exist', () => {
        const {getByText} = render(<{{pascalCase name}}>Funcionou</{{pascalCase name}}>)
        expect(getByText(/Funcionou/)).toBeTruthy()
    });    
});