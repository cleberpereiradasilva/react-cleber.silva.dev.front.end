import { {{pascalCase name}} as {{pascalCase name}}Styled } from "./styles"
import { Props } from "./types"

export const {{pascalCase name}} = ({ children }: Props) => <{{pascalCase name}}Styled>{children}</{{pascalCase name}}Styled>

